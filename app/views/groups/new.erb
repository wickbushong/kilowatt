<h1>Create a new group</h1>

<form method="POST" action="/groups">
    <label>Group name: <input type="text" name="name"></label>
    <br>
    <% if !current_user.devices.empty? %>
        <label>Add devices to this group
        <br>
        <% current_user.devices.each do |device| %>
            <label><input type="checkbox" name="device_ids[]" value="<%= device.id %>"> <%= device.name %>
            </label>
            <br>
        <% end %>
        <br>
    <% end %>
    <input type="submit" value="Create Group">
</form>